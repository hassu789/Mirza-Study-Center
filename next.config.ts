import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
        pathname: '/**',
      },
      {
        protocol: 'https',
        hostname: 'api.dicebear.com',
        pathname: '/**',
      },
    ],
    // Disable optimization to avoid SSL certificate issues
    // Images will load directly from Unsplash without optimization
    unoptimized: true,
  },
};

export default nextConfig;
